# Rust build artefacts
/target

# Coverage profile data. test/coverage.sh writes profraws under
# target/cov/ (already covered above), but failed runs without
# LLVM_PROFILE_FILE land them in CWD as default_*.profraw.
*.profraw
*.profdata

# pgrx run-state (postgres data dirs, configs)
/.pgrx

# OS
.DS_Store
TODO.md
.mcp.json
.claude/settings.local.json

# Personal scratch notes; not part of the source of truth
/evolutions
/bug
/dogfood
/delivery
/review


# ESAC bridge token — never commit
.esac/
.codex/
.claude/
.code-moniker-cache/
